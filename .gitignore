cookies.json
sources.json
.env.config
node_modules/
state.json
logs/
screenshots/
posts.db
uploads/

# DB backups (transient)
posts.db.bak-*
*.db.bak-*

# Local npm / Puppeteer caches (used when global cache dirs are unavailable)
.npm-cache/
.puppeteer-cache/
